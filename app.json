{
  "name": "Poller Server",
  "description": "Throw away polls.",
  "repository": "https://github.com/buren/poller-server",
  "keywords": ["rails", "polls"],
  "env": {
    "RAILS_ENV": "production"
  },
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate"
  }
}
